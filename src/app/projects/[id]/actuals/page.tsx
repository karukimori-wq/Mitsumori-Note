"use client";
import{useMemo,useState}from"react";
type Cost={id:number;type:string;title:string;amount:number};type Work={id:number;date:string;task:string;hours:number};
const yen=(n:number)=>new Intl.NumberFormat("ja-JP",{style:"currency",currency:"JPY",maximumFractionDigits:0}).format(n);
export default function Actuals(){const[works,setWorks]=useState<Work[]>([{id:1,date:"2026-07-08",task:"要件整理",hours:7},{id:2,date:"2026-07-12",task:"トップページ",hours:12},{id:3,date:"2026-07-18",task:"下層ページ",hours:18}]);const[costs,setCosts]=useState<Cost[]>([{id:1,type:"外注費",title:"写真撮影",amount:18000},{id:2,type:"その他",title:"有料素材",amount:4200}]);const[extras,setExtras]=useState([{id:1,title:"追加バナー制作",amount:15000,billable:true}]);const hours=works.reduce((a,w)=>a+w.hours,0),cost=costs.reduce((a,c)=>a+c.amount,0),extra=extras.filter(e=>e.billable).reduce((a,e)=>a+e.amount,0);const summary=useMemo(()=>[{t:"実績作業時間",v:`${hours}h`,s:"想定 56h"},{t:"実績原価",v:yen(cost),s:"想定 ¥25,000"},{t:"追加請求候補",v:yen(extra),s:`${extras.filter(e=>e.billable).length}件`},{t:"現在の実質時間単価",v:hours?yen((320000+extra-cost)/hours)+"/h":"-",s:"暫定値"}],[hours,cost,extra,extras]);return <main className="subpage"><a className="back" href="/projects/1">← 案件概要</a><header><div><h1>実績入力</h1><p>作業時間・原価・追加作業を記録します。</p></div><button>保存</button></header><nav className="tabs"><a href="/projects/1">概要</a><a href="/projects/1/estimate">見積・仕様</a><a href="/projects/1/progress">進捗</a><b>実績</b><span>請求・入金</span><span>答え合わせ</span></nav><section className="summaryCards actualSummary">{summary.map(x=><div key={x.t}><small>{x.t}</small><strong>{x.v}</strong><span>{x.s}</span></div>)}</section><section className="actualGrid"><div className="panel"><div className="sectionTitle"><div><h3>作業時間</h3><p>日別に記録します。</p></div><button className="secondary" onClick={()=>setWorks([...works,{id:Date.now(),date:"2026-07-22",task:"新しい作業",hours:0}])}>＋ 追加</button></div>{works.map((w,i)=><div className="actualRow" key={w.id}><input type="date" value={w.date} onChange={e=>setWorks(works.map((x,n)=>n===i?{...x,date:e.target.value}:x))}/><input value={w.task} onChange={e=>setWorks(works.map((x,n)=>n===i?{...x,task:e.target.value}:x))}/><input type="number" value={w.hours} onChange={e=>setWorks(works.map((x,n)=>n===i?{...x,hours:Number(e.target.value)}:x))}/><span>h</span></div>)}</div><div className="panel"><div className="sectionTitle"><div><h3>実績原価</h3><p>材料・外注・その他経費。</p></div><button className="secondary" onClick={()=>setCosts([...costs,{id:Date.now(),type:"その他",title:"新しい経費",amount:0}])}>＋ 追加</button></div>{costs.map((c,i)=><div className="costRow" key={c.id}><select value={c.type} onChange={e=>setCosts(costs.map((x,n)=>n===i?{...x,type:e.target.value}:x))}><option>材料費</option><option>外注費</option><option>その他</option></select><input value={c.title} onChange={e=>setCosts(costs.map((x,n)=>n===i?{...x,title:e.target.value}:x))}/><input type="number" value={c.amount} onChange={e=>setCosts(costs.map((x,n)=>n===i?{...x,amount:Number(e.target.value)}:x))}/></div>)}</div></section><section className="panel"><div className="sectionTitle"><div><h3>追加作業</h3><p>見積外の作業を記録し、請求書案へ引き継ぎます。</p></div><button className="secondary" onClick={()=>setExtras([...extras,{id:Date.now(),title:"新しい追加作業",amount:0,billable:true}])}>＋ 追加作業</button></div>{extras.map((e,i)=><div className="extraRow" key={e.id}><input value={e.title} onChange={x=>setExtras(extras.map((v,n)=>n===i?{...v,title:x.target.value}:v))}/><input type="number" value={e.amount} onChange={x=>setExtras(extras.map((v,n)=>n===i?{...v,amount:Number(x.target.value)}:v))}/><label><input type="checkbox" checked={e.billable} onChange={x=>setExtras(extras.map((v,n)=>n===i?{...v,billable:x.target.checked}:v))}/> 請求対象</label></div>)}</section></main>}
