"use client";
import{FormEvent,useEffect,useMemo,useState}from"react";
import{addCustomer,getActual,getBilling,getEstimate,loadCustomers,loadProjects,CustomerRecord}from"../lib/storage";
const yen=(n:number)=>new Intl.NumberFormat("ja-JP",{style:"currency",currency:"JPY",maximumFractionDigits:0}).format(n);const daysLate=(due:string)=>due?Math.max(0,Math.floor((Date.now()-new Date(due+"T23:59:59").getTime())/86400000)):0;
export default function Customers(){const[customers,setCustomers]=useState<CustomerRecord[]>([]);const[q,setQ]=useState("");const[open,setOpen]=useState(false);const[name,setName]=useState("");const[person,setPerson]=useState("");const[email,setEmail]=useState("");useEffect(()=>setCustomers(loadCustomers()),[]);const projects=useMemo(()=>loadProjects(),[customers]);const names=useMemo(()=>Array.from(new Set([...customers.map(c=>c.name),...projects.map(p=>p.client)])).map(name=>{const c=customers.find(x=>x.name===name),ps=projects.filter(p=>p.client===name),won=ps.filter(p=>!["見積作成中","見積提出済","失注"].includes(p.status));let sales=0,profit=0,unpaid=0,lateTotal=0,lateCount=0;ps.forEach(p=>{const b=getBilling(p.id),a=getActual(p.id),e=getEstimate(p.id),revenue=b?.lines.reduce((s,x)=>s+x.qty*x.price,0)||p.amount,cost=a?.costs.reduce((s,x)=>s+x.amount,0)||e?.items.reduce((s,x)=>s+x.material+x.outsource+x.other,0)||0;if(p.status!=="失注"){sales+=revenue;profit+=revenue-cost}if(b?.issued&&!b.paid)unpaid+=revenue;if(b?.dueDate){const late=b.paid&&b.paidDate?Math.max(0,Math.floor((new Date(b.paidDate).getTime()-new Date(b.dueDate).getTime())/86400000)):!b.paid?daysLate(b.dueDate):0;if(late>0){lateTotal+=late;lateCount++}}});return{id:c?.id||"",name,person:c?.person||"未登録",email:c?.email||"",projects:ps.length,sales,profit,margin:sales?Math.round(profit/sales*100):0,rate:ps.length?Math.round(won.length/ps.length*100):0,unpaid,repeat:ps.length>=2,avgLate:lateCount?Math.round(lateTotal/lateCount):0}}).sort((a,b)=>b.sales-a.sales),[customers,projects]);const list=names.filter(c=>`${c.name}${c.person}`.toLowerCase().includes(q.toLowerCase())),repeatCount=names.filter(x=>x.repeat).length,topProfit=[...names].sort((a,b)=>b.profit-a.profit)[0],lateCustomers=names.filter(x=>x.avgLate>0).length;function submit(e:FormEvent){e.preventDefault();if(!name.trim())return;const item=addCustomer({name:name.trim(),person,email});setCustomers([item,...customers]);setName("");setPerson("");setEmail("");setOpen(false)}return <main className="subpage"><header><div><h1>顧客</h1><p>売上だけでなく、利益・リピート・未入金・入金傾向まで顧客単位で確認します。</p></div><button onClick={()=>setOpen(!open)}>＋ 新規顧客</button></header><section className="summaryCards"><div><small>顧客数</small><strong>{names.length}</strong></div><div><small>リピート顧客</small><strong>{repeatCount}</strong></div><div><small>最高利益顧客</small><strong>{topProfit?.name||"データなし"}</strong></div><div><small>入金遅延履歴あり</small><strong>{lateCustomers}件</strong></div></section>{open&&<form className="panel customerForm" onSubmit={submit}><h3>新規顧客</h3><div className="formTwo"><label className="formField">顧客名<input required value={name} onChange={e=>setName(e.target.value)} placeholder="会社名・屋号・氏名"/></label><label className="formField">担当者<input value={person} onChange={e=>setPerson(e.target.value)} placeholder="担当者名"/></label></div><label className="formField">メールアドレス<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="example@email.com"/></label><div className="actions"><button type="button" className="secondary" onClick={()=>setOpen(false)}>キャンセル</button><button type="submit">保存</button></div></form>}<section className="panel"><div className="toolbar"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="顧客名・担当者を検索"/><span>{list.length}件</span></div><div className="compareHead"><span>顧客</span><span>売上 / 利益</span><span>リピート・受注</span><span>入金状況</span></div>{list.map(c=>{const body=<><div><strong>{c.name}</strong><small>{c.person}{c.email?` ・ ${c.email}`:""}</small></div><span>{yen(c.sales)} / {yen(c.profit)}（利益率{c.margin}%）</span><span>{c.repeat?`リピート ${c.projects}件`:`案件 ${c.projects}件`}・受注率 {c.rate}%</span><b className={c.unpaid>0||c.avgLate>0?"negative":"positive"}>{c.unpaid>0?`未入金 ${yen(c.unpaid)}`:c.avgLate>0?`平均 ${c.avgLate}日遅延`:"良好"}</b></>;return c.id?<a className="compareRow" href={`/customers/${c.id}`} key={c.name}>{body}</a>:<div className="compareRow" key={c.name}>{body}</div>})}</section><section className="reviewGrid"><div className="panel"><h3>売上上位顧客</h3>{names.slice(0,5).map((c,i)=><div className="riskRow" key={c.name}><strong>{i+1}. {c.name}</strong><span>{c.projects}件</span><b>{yen(c.sales)}</b></div>)}</div><div className="panel"><h3>顧客収益性のヒント</h3><p>{topProfit?`利益額が最も高い顧客は「${topProfit.name}」です。売上規模だけでなく利益率、リピート回数、入金傾向を合わせて優良顧客を判断できます。`:"案件データが増えると顧客別の収益性を分析できます。"}</p>{topProfit&&<b>累計利益 {yen(topProfit.profit)} / 利益率 {topProfit.margin}%</b>}</div></section></main>}
